import React from "react";

import UserInfo from "../components/User Info";

const Profile = (props) => {
  return (
    <main>
      <UserInfo></UserInfo>
    </main>
  );
};
export default Profile;
